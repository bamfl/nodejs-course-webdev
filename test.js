// #1 Базовые концепции и установка (Basic Concepts & Setup Environment)
const userName = 'Dmitriy';

const sayHi = (name) => `My name is ${name}`;

// console.log(sayHi(userName)); // node test.js

module.exports = {
	userName,
	sayHi
};
